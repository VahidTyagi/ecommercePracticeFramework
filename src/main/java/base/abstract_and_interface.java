package base;

public class abstract_and_interface {
	
	// abstract class vs interface
	// abstract class can have method implementations, while interface methods are abstract by default (until Java 8).	
	// abstract class supports single inheritance, while interface supports multiple inheritance.
	
	
	

}
